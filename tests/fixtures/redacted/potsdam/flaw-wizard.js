/* Redacted, synthetic client asset containing only validation literals. */
function configureUpload(){return{accept:"image/png,image/jpeg,image/gif,image/webp",maxFileSizeInMb:8,multiple:true,maxCount:3}}
function createReport(){return request("/api/v1/flaw-reporter/createFlawReport")}
